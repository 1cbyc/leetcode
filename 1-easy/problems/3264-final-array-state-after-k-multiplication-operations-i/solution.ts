function getFinalState(nums: any, k: any, multiplier: any): boolean | number | string | any {
  """
  :type nums: List[int]
  :type k: int
  :type multiplier: int
  :rtype: List[int]
  """
  EPS = 1e-15
  def count(x, target):
      return int(target-x+EPS);
}

export { getFinalState };
