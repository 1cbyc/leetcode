function minimumPairRemoval(nums: any): boolean | number | string | any {
  """
  :type nums: List[int]
  :rtype: int
  """
  def add(i):
      if (0 <= i < right[i] < (nums).length) {
          sl.add([nums[i]+nums[right[i]], i])
          if (nums[i] > nums[right[i]]) {
              cnt[0] += 1
}

export { minimumPairRemoval };
