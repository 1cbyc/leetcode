function hasGroupsSizeX(deck: any): boolean | number | string | any {
  """
  :type deck: List[int]
  :rtype: bool
  """
  def gcd(a, b):  # Time: O((logn)^2)
      while (b) {
          a, b = b, a % b
      return a;
}

export { hasGroupsSizeX };
