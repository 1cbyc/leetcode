def countSegments(s):
    """
    Count the number of segments (words) in a string.

    Args:
        s: str - input string

    Returns:
        int - number of segments
    """
    return len(s.split())
